# shadow-fight 游戏代码编码规范

## 类型安全

- **禁止使用字面量字符串作为类型标识**。所有分类值、状态标识、Buff 类型等必须定义为枚举（enum），通过枚举引用使用。
  - 正确：`BuffType.ATK_DEBUFF`
  - 错误：`'ATK_DEBUFF'`
- 新增 Buff 类型时，在 `types/Enums.ts` 的 `BuffType` 枚举中添加值。
- 新增其他分类标识时，在 `types/Enums.ts` 或对应的类型文件中添加枚举值。

## 架构分层

- `core/` 层为纯 TypeScript 逻辑，**禁止引用 Cocos Creator API**（cc 模块）。
- `ui/` 层负责将 core 状态映射到可视化，可引用 cc 模块。
- `managers/` 层为 Cocos 组件，负责生命周期管理和场景调度。
- `data/` 层存放静态配置数据，只包含数据定义，不包含逻辑。

## 命名约定

- 文件名：PascalCase（如 `BattleEngine.ts`）
- 枚举值：UPPER_SNAKE_CASE（如 `ATK_BONUS`）
- 接口名：PascalCase，不加 I 前缀（如 `RuntimeCombatant`，不是 `IRuntimeCombatant`）
- 函数名：camelCase（如 `getEffectiveSpeed`）
